# ============================================================================
# EXAMPLE METRICS - Delete this file once you've created your own
# ============================================================================
# These examples show proper metric syntax. Uncomment and modify as needed.
# Documentation: https://github.com/WhoopInc/snowflake-semantic-tools/blob/main/docs/semantic-models-guide.md
#
# snowflake_metrics:
#   # -------------------------------------------------------------------------
#   # Simple Aggregation Metric
#   # -------------------------------------------------------------------------
#   - name: total_revenue
#     description: Total revenue from all orders
#     tables:
#       - {{ table('orders') }}
#     expr: SUM({{ column('orders', 'amount') }})
#
#   # -------------------------------------------------------------------------
#   # Count Distinct Metric
#   # -------------------------------------------------------------------------
#   - name: unique_customers
#     description: Number of unique customers
#     tables:
#       - {{ table('customers') }}
#     expr: COUNT(DISTINCT {{ column('customers', 'customer_id') }})
#
#   # -------------------------------------------------------------------------
#   # Metric with Multiple Tables
#   # -------------------------------------------------------------------------
#   - name: revenue_per_customer
#     description: Average revenue per customer
#     tables:
#       - {{ table('orders') }}
#       - {{ table('customers') }}
#     expr: SUM({{ column('orders', 'amount') }}) / NULLIF(COUNT(DISTINCT {{ column('customers', 'customer_id') }}), 0)
#
#   # -------------------------------------------------------------------------
#   # Ratio/Percentage Metric
#   # -------------------------------------------------------------------------
#   - name: order_completion_rate
#     description: Percentage of orders that were completed
#     tables:
#       - {{ table('orders') }}
#     expr: |
#       COUNT(CASE WHEN {{ column('orders', 'status') }} = 'completed' THEN 1 END) * 100.0 /
#       NULLIF(COUNT(*), 0)
#
#   # -------------------------------------------------------------------------
#   # Running Total / Window Metric
#   # -------------------------------------------------------------------------
#   - name: cumulative_revenue
#     description: Running total of revenue over time
#     tables:
#       - {{ table('orders') }}
#     expr: SUM(SUM({{ column('orders', 'amount') }})) OVER (ORDER BY {{ column('orders', 'order_date') }})

