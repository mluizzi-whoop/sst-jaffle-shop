# ============================================================================
# EXAMPLE FILTERS - Delete this file once you've created your own
# ============================================================================
# These examples show proper filter syntax. Uncomment and modify as needed.
# Documentation: https://github.com/WhoopInc/snowflake-semantic-tools/blob/main/docs/semantic-models-guide.md
#
# snowflake_filters:
#   # -------------------------------------------------------------------------
#   # Simple Equality Filter
#   # -------------------------------------------------------------------------
#   - name: active_customers_only
#     description: Filter to only include active customers
#     tables:
#       - {{ table('customers') }}
#     expr: {{ column('customers', 'status') }} = 'active'
#
#   # -------------------------------------------------------------------------
#   # Date Range Filter
#   # -------------------------------------------------------------------------
#   - name: last_30_days
#     description: Filter to orders from the last 30 days
#     tables:
#       - {{ table('orders') }}
#     expr: {{ column('orders', 'order_date') }} >= DATEADD(day, -30, CURRENT_DATE())
#
#   # -------------------------------------------------------------------------
#   # Current Year Filter
#   # -------------------------------------------------------------------------
#   - name: current_year
#     description: Filter to current calendar year
#     tables:
#       - {{ table('orders') }}
#     expr: YEAR({{ column('orders', 'order_date') }}) = YEAR(CURRENT_DATE())
#
#   # -------------------------------------------------------------------------
#   # Exclusion Filter
#   # -------------------------------------------------------------------------
#   - name: exclude_test_accounts
#     description: Exclude test and internal accounts
#     tables:
#       - {{ table('customers') }}
#     expr: |
#       {{ column('customers', 'email') }} NOT LIKE '%@test.com'
#       AND {{ column('customers', 'is_internal') }} = FALSE
#
#   # -------------------------------------------------------------------------
#   # Multi-Table Filter
#   # -------------------------------------------------------------------------
#   - name: high_value_orders
#     description: Orders over $100 from premium customers
#     tables:
#       - {{ table('orders') }}
#       - {{ table('customers') }}
#     expr: |
#       {{ column('orders', 'amount') }} > 100
#       AND {{ column('customers', 'tier') }} = 'premium'

